import alt from '../alt';

class AddCharacterActions {
  constructor() {
    this.generateActions('AddCharacterActions')
  }

  action(props) {
    // $.ajax({'type':'POST', 'url':'/URL', 'data':'data'});
  }
}
export default alt.createActions(AddCharacterActions);
